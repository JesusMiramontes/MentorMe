<header>
  <link href="https://fonts.googleapis.com/css?family=Roboto:300,400,700" rel="stylesheet">
  <nav class="be-blue white large-padding bold-font">
    <ul style="list-style-type: none" class="no-list row center-xs middle-xs">
      <li class="col-xs ">
        <h1 class="no-marging no-padding" id="logo"><%= link_to "MentorMe", root_path %></h1>
      </li>
      </li>
      <% if user_signed_in? %>
        <li class="col-xs">
          <%= link_to "Cerrar sesión", destroy_user_session_path, method: :delete%>
        </li>
      <%else%>
        <li class="col-xs">
          <%= link_to "Iniciar sesión", new_user_session_path%>
        </li>
        <li class="col-xs">
          <%= link_to "Registro", new_user_registration_path%>
        </li>
      <%end%>
    </ul>
  </nav>
</header>